| Parameter                                       | Current value | Recommended range to try |                                            Recommended value | Comment                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------- | ------------: | -----------------------: | -----------------------------------------------------------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `actor_num_simulation`                          |          `50` |                `200–800` |                                               `200` to start | This is the main search-budget knob. MiniZero’s default is `50`, but MiniZero’s own docs show larger-board examples above toy TicTacToe settings, and the AlphaZero paper used **800 simulations** during training. For your game, `200` is a good first “serious but not crazy” value. ([GitHub][1])                       |
| `nn_num_blocks`                                 |           `1` |                    `4–8` |                                                          `4` | `1` block is okay for a smoke test, but probably small for a 12-cell-side hex board and 6-in-a-row tactics. AlphaZero used **19 residual blocks**; you likely do not need that much, but `4` is a much more plausible starting point. ([GitHub][1])                                                                         |
| `nn_num_hidden_channels`                        |         `256` |                `128–256` |              `128` if you raise blocks, otherwise keep `256` | AlphaZero used **256 filters**, so your current width is fully defensible. The reason to change it is mostly compute budget: once you increase depth, `128` can be a nicer balance. ([GitHub][1])                                                                                                                           |
| `nn_num_value_hidden_channels`                  |         `256` |                `128–256` |                                             match main width | AlphaZero’s value head used a **256-unit hidden layer**, so `256` is also a reasonable keep-as-is value. I’d mostly change this only to stay aligned with the main network width. ([GitHub][1])                                                                                                                             |
| `actor_select_action_softmax_temperature_decay` |       `false` |         `false` / `true` |                                                       `true` | Worth trying because AlphaZero training sampled moves from the **root visit counts**, while evaluation was greedy by visit count. Turning decay on is a practical way to keep exploration early and sharpen play later. ([GitHub][1])                                                                                       |
| `actor_select_action_softmax_temperature`       |           `1` |                `0.5–1.0` | `1.0` if decay is on; otherwise `0.5–0.75` later in training | `1` is a standard exploratory choice. I would only cool it down if late-training self-play still looks noisy or random. AlphaZero’s training move selection was proportional to root visit count rather than greedy. ([GitHub][1])                                                                                          |
| `actor_dirichlet_noise_alpha`                   |        `0.03` |              `0.02–0.05` |                                                       `0.03` | Since your action space is **one action per cell**, `0.03` is actually a pretty nice default: MiniZero documents this parameter as typically scaling like `1/sqrt(#actions)`, and AlphaZero used **0.03 for Go**. I would change it only if openings are clearly too repetitive or too chaotic. ([GitHub][1])               |
| `actor_dirichlet_noise_epsilon`                 |        `0.25` |              `0.20–0.30` |                                                       `0.25` | This is a standard root-noise mixing value. It is worth tuning only if self-play feels too deterministic or too random. ([GitHub][1])                                                                                                                                                                                       |
| `actor_use_random_rotation_features`            |        `true` |         `false` / `true` |           `true`, assuming your symmetry handling is correct | For a symmetric hex-board game this is a sensible thing to keep on. The main reason to reconsider it is implementation risk: only keep it if rotations/reflections are mapped correctly for both inputs and policy targets. The AlphaZero supplement explicitly discusses symmetry-augmented variants for Go. ([GitHub][1]) |
| `zero_num_games_per_iteration`                  |        `2000` |              `1000–4000` |                                                       `2000` | This is a throughput/stability knob rather than an obvious fix. Keep it unless iterations are too slow or updates look too noisy. MiniZero’s docs present small toy runs and larger board-game runs, but do not make this parameter look obviously wrong at `2000`. ([GitHub][1])                                           |
| `zero_end_iteration`                            |         `100` |               `150–300+` |                                                        `200` | `100` is a fine first serious run, but I would be ready to go longer if the Elo/win-rate curves are still rising. MiniZero’s docs use much shorter runs for toy TicTacToe and longer ones for harder settings. ([GitHub][1])                                                                                                |
